export const SnapshotUploadPage = () => {
  return (
    <section className="kp-page">
      <h1>Snapshot Upload</h1>
      <p>Upload HTML snapshots and start snapshot runs from here.</p>
    </section>
  );
};
